import styled from 'styled-components';

export const Label = styled.label`
  display: block;
  font-size: 1.4rem;
  font-weight: 500;
  font-style: italic;
  margin-bottom: 4px;
  color: var(--text);
`;
